{
  "name": "paka-intelligence-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'dev script not configured' && exit 1",
    "build": "echo 'build script not configured' && exit 1",
    "start": "echo 'start script not configured' && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}
